{
  "name": "node-challenge-bonus",
  "version": "0.1.0",
  "description": "",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "jest --forceExit --coverage --verbose",
    "tslint": "tslint -c tslint.json -p tsconfig.json",
    "watch:ts": "tsc -w",
    "watch:test": "npm run test -- --watchAll"
  },
  "devDependencies": {
    "@types/jest": "^22.1.3",
    "@types/node": "^9.4.6",
    "chai": "^4.1.2",
    "jest": "^22.0.4",
    "supertest": "^3.0.0",
    "ts-jest": "^23.10.5",
    "tslint": "^5.9.1",
    "typescript": "^3.2.2"
  }
}
